# Port the Socket.IO server listens on
PORT=8000

# Allowed client origins for CORS (the URLs your frontend is served from).
# Add as many as you need; unused ones can be left blank.
CLIENT_1_URL=http://127.0.0.1:5500
CLIENT_2_URL=
CLIENT_3_URL=
